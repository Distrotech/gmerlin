if HAVE_VORBIS
vorbis_subdirs = vorbis
else
vorbis_subdirs =
endif

if HAVE_FLAC
flac_subdirs = flac
else
flac_subdirs =
endif

if HAVE_LAME
lame_subdirs = lame
else
lame_subdirs =
endif

if HAVE_FAAC
faac_subdirs = faac
else
faac_subdirs =
endif

SUBDIRS = $(vorbis_subdirs) $(flac_subdirs) $(lame_subdirs) $(faac_subdirs)

